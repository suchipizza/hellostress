# Reference Solution

This case uses the same public hand calculation documented in `examples/hand_calc_comparison/hand_calc.md`:

- `I = b h^3 / 12`
- `delta_max = F L^3 / (3 E I)`
- `sigma_max = F L (h / 2) / I`

For the committed prompt, the expected values are:

- `max_deflection = 3.0e-05 m`
- `max_stress = 9.0e05 Pa`
