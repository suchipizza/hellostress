# Public Formula Checks

This category is for reproducible comparisons against public hand calculations or formula-derived expected results.

Current committed case:

- `cantilever_beam_hand_calc/`: verifies the current `mock` cantilever beam output against the same closed-form hand calculation documented in `examples/hand_calc_comparison/`.
