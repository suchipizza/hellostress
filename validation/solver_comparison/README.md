# Solver Comparison Intake

This directory is intentionally a scaffold.

Use it when the same supported prompt is ready to be compared across:

- `mock`
- Docker-backed generated DOLFINx
- any future backend that preserves the current artifact contract

Do not describe this directory as a finished benchmark until at least one reproducible comparison case, reference interpretation note, and tolerance policy are committed.
