# hellostress
**FEA Copilot** is a web-based assistant that allows users to set up and run basic Finite Element Analysis (FEA) simulations using **natural language prompts**.   The system converts user input into a **FEniCS Python script**, runs the simulation in the backend, and displays the results interactively (plots + summaries).
