# Add a workspace policy example

Label suggestions: `good first issue`, `example`, `documentation`

Create an example that demonstrates export-ready versus blocked artifact bundles using the current inspection and export CLI paths.
