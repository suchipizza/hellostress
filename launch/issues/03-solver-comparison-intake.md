# Add a first solver-comparison intake case

Label suggestions: `good first issue`, `validation`, `benchmark`

Use `validation/solver_comparison/` to compare one supported beam prompt across `mock` and Docker modes, with a note explaining which differences are expected.
