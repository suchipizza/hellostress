# Add a solver-adapter template fixture

Label suggestions: `good first issue`, `solver adapter`, `help wanted`

Create a lightweight test fixture that validates the expected artifact keys for a new backend adapter scaffold.
