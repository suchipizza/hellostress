from __future__ import annotations

from pathlib import Path


def export_bundle(bundle_path: Path, output_dir: Path) -> Path:
    raise NotImplementedError("TODO: implement exporter contract for artifact bundles.")
